version: '3'
services:
    dnscrypt-proxy:
        image: klutchell/dnscrypt-proxy
    drill:
        image: alpine
        command: sh -c 'apk add -q --no-cache drill && drill -p 5053 dnscrypt.info @dnscrypt-proxy'
        links:
            - dnscrypt-proxy