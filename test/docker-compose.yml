version: '3'
services:
    dnscrypt-proxy:
        image: klutchell/dnscrypt-proxy
    dig:
        image: alpine
        command: sh -c 'apk add -q --no-cache bind-tools && dig @dnscrypt-proxy -p 5053 dnscrypt.info AAAA'
        links:
            - dnscrypt-proxy